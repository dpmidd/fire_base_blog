<aside>
  {{#each blog in model}}
    <h6>{{blog.title}}</h6>
  {{/each}}
</aside>

<main>
  <div>
    {{textarea value=title}}
  </div>
  <div>
    {{textarea value=body}}
  </div>
  <button {{action 'saveBlog'}}>Save</button>
</main>

{{outlet}}
